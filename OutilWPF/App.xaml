<Application x:Class="OutilWPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OutilWPF">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="Color.Background">#FFF5F1E8</Color>
            <Color x:Key="Color.Surface">#FFFFFCF7</Color>
            <Color x:Key="Color.SurfaceStrong">#FFE7EFE8</Color>
            <Color x:Key="Color.Accent">#FF0E5A4F</Color>
            <Color x:Key="Color.AccentSoft">#FFB7D5CF</Color>
            <Color x:Key="Color.Ink">#FF20312C</Color>
            <Color x:Key="Color.Border">#FFD6DBD4</Color>

            <SolidColorBrush x:Key="Brush.Background" Color="{StaticResource Color.Background}" />
            <SolidColorBrush x:Key="Brush.Surface" Color="{StaticResource Color.Surface}" />
            <SolidColorBrush x:Key="Brush.SurfaceStrong" Color="{StaticResource Color.SurfaceStrong}" />
            <SolidColorBrush x:Key="Brush.Accent" Color="{StaticResource Color.Accent}" />
            <SolidColorBrush x:Key="Brush.AccentSoft" Color="{StaticResource Color.AccentSoft}" />
            <SolidColorBrush x:Key="Brush.Ink" Color="{StaticResource Color.Ink}" />
            <SolidColorBrush x:Key="Brush.Border" Color="{StaticResource Color.Border}" />

            <Style TargetType="{x:Type Window}">
                <Setter Property="Background" Value="{StaticResource Brush.Background}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Ink}" />
                <Setter Property="FontFamily" Value="Segoe UI" />
            </Style>

            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Ink}" />
            </Style>

            <Style TargetType="{x:Type Control}" x:Key="fontStyling">
                <Setter Property="FontSize" Value="14" />
            </Style>

            <Style TargetType="{x:Type Button}">
                <Setter Property="Padding" Value="14,8" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                <Setter Property="Background" Value="{StaticResource Brush.Surface}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Ink}" />
            </Style>

            <Style x:Key="ActionButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{StaticResource Brush.Accent}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Accent}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style x:Key="SubtleButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{StaticResource Brush.SurfaceStrong}" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                <Setter Property="Background" Value="White" />
            </Style>

            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                <Setter Property="Background" Value="White" />
            </Style>

            <Style TargetType="{x:Type DataGrid}">
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                <Setter Property="GridLinesVisibility" Value="Horizontal" />
                <Setter Property="RowBackground" Value="White" />
                <Setter Property="AlternatingRowBackground" Value="#FFF7F4EE" />
                <Setter Property="HeadersVisibility" Value="All" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
